name: DEPRECATED Build iOS IPA (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow retired
        run: |
          echo "::error::该工作流已退场，请改用: Build iOS ad-hoc / Build iOS TestFlight / Build iOS AppStore Upload"
          exit 1