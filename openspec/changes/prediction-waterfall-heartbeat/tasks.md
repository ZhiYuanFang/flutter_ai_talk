## 1. 瀑布流容器

- [x] 1.1 紧凑态由固定比例 `GridView` 改为双列瀑布流（优先无新依赖）；卡片高度随内容
- [x] 1.2 确认默认仍为紧凑、列表/紧凑本地记忆键兼容（`grid`＝瀑布流）

## 2. 卡片结构

- [x] 2.1 compact：标题行仅名+开关；倒计时上方居中更大 `EventLogo`；倒计时区去掉依赖父级 `Expanded` 的死约束
- [x] 2.2 列表态折线卡结构保持不变

## 3. 心跳

- [x] 3.1 计算未超时、推演开、可预测中 `nextAt` 最早的 `eventId`
- [x] 3.2 仅该卡大图持续心跳缩放；无候选则无动画

## 4. 验收

- [x] 4.1 紧凑：双列错落；大图在倒计时上；最近未超时事件心跳
- [x] 4.2 列表切换与重启后偏好仍正确
