# remember-select-event-in-form 任务清单

## 1. 存储与状态接入

- [x] 1.1 新增趋势事件选择记忆 store（SharedPreferences 单键，含 `load`/`save`）。
- [x] 1.2 在趋势页初始化流程接入记忆读取，按“记忆优先、叶子兜底”计算初始 `_selectedKey`。
- [x] 1.3 在事件选择器确认切换后写入记忆，并保持现有趋势序列加载时序正确。

## 2. 回退与刷新一致性

- [x] 2.1 调整目录刷新监听逻辑：当选中项失效并回退时，同步更新记忆值。
- [x] 2.2 保证仅在选中项实际变化时触发拉数，避免重复请求或闪烁。
- [x] 2.3 兼容目录为空/记忆无效等边界，确保页面可用且有稳定兜底。

## 3. 验证与回归

- [ ] 3.1 手动验证场景：首次进入无记忆、二次进入有记忆、记忆失效回退、目录刷新后回退。
- [x] 3.2 回归验证不受影响能力：仅叶子可选、日期范围记忆、登录蒙层与图表展示。
- [x] 3.3 更新变更说明与验收记录，确保任务与规格场景一一对应。

## 4. 验收记录（本次会话）

- 代码变更：新增 `TrendsSelectedEventStore`，并在 `TrendsScreen` 完成初始化恢复、切换写入、刷新回退后同步写入。
- 修复补充：初始化阶段新增 `_selectionReady` 门闩，避免目录加载时先回退并覆盖记忆；初始化完成后统一触发一次趋势加载。
- 对应规格：
  - `trends-selected-event-memory`：覆盖“切换后写入”“再次进入优先恢复”“记忆失效回退”。
  - `trends-leaf-event-picker`（modified）：覆盖“记忆优先，否则第一个叶子”及“选中项失效回退”。
- 校验结果：已对改动文件执行静态错误检查（无错误）。
- 自动化回归：已执行测试集，结果为 8 passed / 0 failed。
- 待人工验证：3.1 需在真机/模拟器交互场景下完成最终验收。
