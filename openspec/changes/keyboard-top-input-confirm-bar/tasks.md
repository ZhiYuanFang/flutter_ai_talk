# Tasks: keyboard-top-input-confirm-bar

## 1. 输入清单与接入基线

- [x] 1.1 整理并确认全量输入入口清单（页面、对话框、底部弹层），标注目标 `TextEditingController` 与提交流程映射。
- [x] 1.2 为首批接入范围建立验收基线（首页文本输入、登录、注册），明确“键盘不顶起 + 确认回填 + 提交触发”通过标准。

## 2. 全局输入桥接与确认条组件

- [x] 2.1 实现全局输入桥接状态容器（焦点目标、临时输入值、确认回调、场景元信息）。
- [x] 2.2 实现键盘顶部输入确认条组件（左侧文本区、右侧确定按钮、键盘可见时展示）。
- [x] 2.4 确认条在无输入时展示场景 hint，说明当前输入框所代表的内容；有输入时展示实际内容（密码仍脱敏）。
- [x] 2.3 增加密码场景脱敏策略，确保确认条不明文显示敏感输入。

## 3. 布局稳定策略改造

- [x] 3.1 在首批页面接入“聚焦后不顶起主界面”策略，并保持原视觉结构稳定。
- [x] 3.2 在弹层场景接入同策略，处理键盘高度变化与可操作区域边界。
- [x] 3.3 验证 `KeyboardDismissScope` 与新桥接行为不冲突，确保失焦和收键盘行为一致。

## 4. 输入回填与提交流程打通

- [x] 4.1 在首页文本输入场景接入确认条回填与提交回调映射。
- [x] 4.2 在登录/注册场景接入确认条回填与提交回调映射，保持原校验与按钮行为。
- [x] 4.3 在设置弹窗、记录编辑与数值备注等弹层输入场景接入确认条回填与提交回调映射。
- [x] 4.4 在宝宝绑定与资料编辑场景接入确认条并验证与原保存流程兼容。

## 5. 回归验证与发布保障

- [x] 5.1 补充关键输入路径测试用例（聚焦展示、布局稳定、确定回填、提交触发、密码脱敏）。
- [ ] 5.2 执行 Android/iOS 端到端回归，覆盖键盘弹出/收起、焦点切换、弹层关闭等边界场景。
- [x] 5.3 增加按页面回退策略说明（出现严重问题时可关闭接入并恢复旧行为）。
