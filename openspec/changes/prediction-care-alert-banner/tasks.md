## 1. 规则引擎与期望表

- [x] 1.1 新增 `prediction_care_alert.dart`：候选类型、原因结构体、偏离分数与 Top1 选取（优先级：间隔拉长 > 进行中过久 > 突然消失）
- [x] 1.2 实现自身 7 日基线计算（中位间隔、P75 时长、日均次数）与样本门槛（间隔拉长 ≥3）
- [x] 1.3 新增本地月龄期望表（0–3 / 4–6 / 7–12 / 12+）；未知月龄或未匹配类型仅用自身基线
- [x] 1.4 实现三条规则阈值判定（对齐 design D3），输出可序列化/可传路由的 `CareAlertReason`

## 2. Provider 与路由

- [x] 2.1 新增只读 Riverpod provider：聚合 range 历史 + 宝宝档案 → Top1（无副作用 HTTP）
- [x] 2.2 注册 `/prediction/alert` 详情路由，支持从 Banner 传入结构化 reason（extra）

## 3. 智能预测页 UI

- [x] 3.1 在 tip 区与事件列表之间插入「值得留意」Banner；无 Top1 时整块隐藏
- [x] 3.2 Banner 点击 push 详情页；详情展示类型 / 自身基线 / 月龄期望 / 实际观测（非诊断文案）
- [x] 3.3 玻璃拟化视觉与现有预测页 token 对齐

## 4. 验收

- [x] 4.1 手动验证：有间隔拉长时压过其他类型；无候选时 Banner 不占位；未知月龄仍可出自身基线预警
- [x] 4.2 确认评估路径未新增 HTTP；推演开关 OFF 仍参与扫描（与 design 一致）
