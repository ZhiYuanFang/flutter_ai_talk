## 1. 草稿与上一步

- [x] 1.1 按根缓存上次时间、间隔与首次默认快照；切页恢复草稿，不得覆盖已有草稿
- [x] 1.2 非首张事件表单左下「上一步」回到上一张表单；第一张、思考盖层、收尾页不展示

## 2. 跳过与确认拦截

- [x] 2.1 标题行：EventLogo + 事件名（可省略）+「跳过」；移除底栏「不记得了，跳过」；跳过仍关推演且不播思考
- [x] 2.2 确认成功时对该根 `setEnabled(true)`；未改快照则不写种子、不播思考，确认按钮上方红字「请认真回忆事件」

## 3. 时间 Sheet 与子事件

- [x] 3.1 新增单层玻璃时分/日期切换 Sheet（默认时分，左上角切换，确定 clamp ≤ now）；`_pickLastAt` 改调之，不再连环两层 Sheet
- [x] 3.2 「该事件包含」改为 EventLogo + 名称只读展示，去掉 Chip/按钮形态

## 4. 校验

- [x] 4.1 `dart analyze` 触及文件无新增 error
- [x] 4.2 手工：上一步草稿、首张/思考/收尾无上一步、跳过同行、未改拦截、改后确认、时间单层默认时分、子事件 logo
- [x] 4.3 未改 `app/android/**` 则跳过 release；若触及原生再补 `flutter build apk --release` 与 proguard
