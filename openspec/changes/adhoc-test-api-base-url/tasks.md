# Tasks

## 1. CI 注入

- [x] 1.1 在 `ios-build-core.yml` 的 ad-hoc 分支为 `flutter build ipa` 注入 `API_BASE_URL=https://test.pangbao.cuplay.top` 并输出构建日志

## 2. 文档

- [x] 2.1 在 `docs/github-ios-ipa.md` 说明 ad-hoc 产物连接测试网关、其他入口保持生产默认
