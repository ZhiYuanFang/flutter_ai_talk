## 1. 布局状态与去开关

- [x] 1.1 新增布局偏好 store/provider（`list` / `grid`，默认 `grid`，本地持久化）
- [x] 1.2 标题行右上角可变图标切换布局；图标随当前模式变化
- [x] 1.3 移除每事件「推演」Switch；预测页/行构建/首页 tip 不再消费推演关闭集合

## 2. 纵向与网格卡片

- [x] 2.1 纵向：相对时间去掉 `下次 ·` 前缀，保留原相对时间正文；7 日图 + Y 轴保持既有行为
- [x] 2.2 网格：双列卡片；短文案（`超时 x 分钟` / `x 小时后` 类）
- [x] 2.3 网格图：X 窗为前天/昨天/今天；隐藏 Y 轴；取点规则沿用 TOD-near / 今日 nextAt 于 3 日窗

## 3. 并存与验收

- [x] 3.1 与留意跑马灯并存：tip → 跑马灯 → 卡片区；网格时跑马灯仍全宽不进格子
- [x] 3.2 手动验证：默认网格、切换持久化、无推演开关、纵向无「下次 ·」、网格三日无 Y
