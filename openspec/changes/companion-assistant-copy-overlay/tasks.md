## 1. 修复展示回归

- [x] 1.1 助手气泡去掉 `clipBehavior: Clip.none`，恢复默认玻璃裁剪
- [x] 1.2 去掉助手外层 `SelectionArea` / `contextMenuBuilder`（拆除 C′a 主路径）
- [ ] 1.3 确认热重启后用户与助手气泡均可见

## 2. 方案 C′b：长按上方「复制」

- [x] 2.1 助手完成态气泡支持长按，在按压位置上方展示 Overlay「复制」
- [x] 2.2 点击「复制」写入剪贴板（优先选中片段，否则整段 `answer`）+ toast「已复制」并关闭 Overlay
- [x] 2.3 Overlay 在滚动、点空白、dispose 时关闭，无泄漏

## 3. 回归

- [ ] 3.1 手工：气泡可见；长按助手出「复制」；剪贴板正确
- [ ] 3.2 手工：用户气泡选区仍可用；首页 tip 仍不可选；列表可竖滑
