# Tasks: disable-default-input-autofocus

## 1. 输入入口梳理与基线确认

- [x] 1.1 盘点所有包含文本输入的页面与弹层，标注当前是否存在默认聚焦行为。
- [x] 1.2 建立验收基线：进入页面与返回页面时键盘不得自动弹出。

## 2. 焦点策略改造

- [x] 2.1 移除或禁用输入控件上的默认 `autofocus` 行为。
- [x] 2.2 在路由进入与返回时增加统一焦点清理，避免输入框被动获取焦点。
- [x] 2.3 确保仅用户显式点击输入框时才触发聚焦与键盘弹出。

## 3. 场景联调与回归

- [x] 3.1 验证首页、登录、注册、设置弹窗、历史编辑等场景在进入/返回时不自动弹键盘。
- [x] 3.2 验证原有输入校验、提交按钮、`onSubmitted` 等业务行为保持一致。
- [x] 3.3 补充回退说明：若出现异常，按页面恢复原焦点策略并记录影响范围。
