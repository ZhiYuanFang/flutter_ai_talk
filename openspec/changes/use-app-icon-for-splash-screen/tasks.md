# Tasks: use-app-icon-for-splash-screen

## 1. 资源与样式对齐

- [x] 1.1 确认桌面主图标同源资源路径，并将启动页图标引用切换到该资源
- [x] 1.2 在启动图标容器中应用圆形裁剪与填满策略（如 `ClipOval` + `BoxFit.cover`）
- [x] 1.3 清理或标注旧 `splash_logo` 在启动页中的引用，避免双源并存

## 2. 冷启动与 /splash 一致性实现

- [x] 2.1 统一 `StartupBrandingOverlay` 与 `/splash` 占位页的图标展示组件或样式常量
- [x] 2.2 保持现有背景渐变、标语与启动时序逻辑不变，仅替换视觉层图标呈现
- [x] 2.3 补充必要注释，说明“桌面图标同源 + 圆形填满”的实现意图

## 3. 验证与回归

- [x] 3.1 在 Android/iOS 冷启动场景验证中心图标为圆形填满且无明显留白
- [x] 3.2 验证 `/splash` 与启动遮罩图标来源与样式一致，无视觉跳变
- [x] 3.3 运行现有检查（静态分析/测试）并记录结果，确认无启动时序回归
- [x] 3.4 将 Android 原生启动图（Android 12+ 与 12 以下）切换为桌面图标同源资源
- [x] 3.5 修复 Android 原生启动图标非圆形问题（12 以下圆形底板，12+ 前景图 + 背景色）
- [x] 3.6 修复 Android 12 以下原生启动页图标未占满圆形底板的问题（前景尺寸与圆底一致）
- [x] 3.7 使用桌面图标本体作为 Android 原生启动图标，消除前景层留白导致的未占满问题
- [x] 3.8 新增 `assets/images/app_icon_round.png` 并统一替换原生启动页与应用内桌面图标展示来源
