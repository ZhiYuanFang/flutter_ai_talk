# Tasks: ucg-square-feed-media-to-detail

- [x] 1.1 移除 `_MasonryMedia` 图片 `GestureDetector` 与 lightbox 调用，点击由 card `onTap` 进详情
- [x] 1.2 视频区改为轻量静态封面 + 播放图标，不在 Feed init 内联播放器
- [x] 1.3 清理 `ucg_masonry_feed_card.dart` 中不再使用的 import
