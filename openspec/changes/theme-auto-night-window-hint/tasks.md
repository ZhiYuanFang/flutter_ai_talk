## 1. UI

- [x] 1.1 在 `theme_palette_sheet.dart` 将「自动夜空」改为标题 + 下方小字 `19:00–05:00` + Switch 的布局（小字始终显示，样式更淡更小）
- [x] 1.2 确认开关开/关时小字均可见，且与 Sheet 顶栏 Row 对齐正常

## 2. 验收

- [x] 2.1 手动打开主题 Sheet：核对文案精确为 `19:00–05:00`；切换自动夜空后小字仍在
