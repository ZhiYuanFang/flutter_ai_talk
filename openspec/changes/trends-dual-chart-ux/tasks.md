## 1. 范围预设与数据模型

- [x] 1.1 将范围逻辑改为预设 n∈{7,15,30}→本地起止日；默认近7日；停止读写 `TrendsDateRangeStore`
- [x] 1.2 将日期入口 Sheet 改为三选项底部选择（近7日/近15日/近1个月），移除自由日历与近3个月
- [x] 1.3 扩展 piece 加载：保留 raw 发生点，并派生日桶序列供近 N 日图与选中日过滤

## 2. 页面壳与选中日

- [x] 2.1 重构 `TrendsScreen`：选择行 `[logo][事件名 ▾]` + 右侧预设文案；去掉上方大 logo
- [x] 2.2 实现纵屏上下 / 横屏左右双图均分，以及各区域内居中、事件色图标题（含 logo）
- [x] 2.3 实现选中日状态机（默认今日、不记忆、换事件保持、换范围越界回今日、点柱切换）
- [x] 2.4 将 logo 飞入落点改为选择行左侧 `EventLogo` 的 `GlobalKey`

## 3. 近 N 日柱图

- [x] 3.1 近 N 日柱图展示 Y 轴且固定 3 刻度；横轴按日
- [x] 3.2 柱顶量标（时分 / unit /「次」）仅选中柱显示
- [x] 3.3 计次事件柱内按 24h 比例绘制深色散点

## 4. 某日详图

- [x] 4.1 计时/计数：0–24 折线 + 折点量标（避挡折线）+ Y 轴 3 刻度
- [x] 4.2 计次：时间轴（无数值 Y）+ 四时段底（与 accent 交叉）+ 发生点下细线与 `HH:mm`
- [x] 4.3 选中日无数据时渲染图表骨架，禁止空态文案

## 5. 验收

- [x] 5.1 手工验证：三预设、三类事件、纵/横屏、选中日规则、骨架、飞入落点
- [x] 5.2 确认未新增 `**/test/**`；未改 `app/android/**`（无需 release APK）
