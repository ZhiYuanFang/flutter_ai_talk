## 1. 共享加事件入口

- [x] 1.1 从 `HomeScreen` 抽出 `handleEventGridTap`（含子 picker、三类型、remote gate、计时守卫、提交、历史乐观更新、usage 计数、单飞）
- [x] 1.2 `HomeScreen` 事件格改为调用共享入口，行为与现网一致

## 2. 预测页网格接线

- [x] 2.1 网格/`compact` 卡片整卡可点 → 共享入口；Switch 不触发添加
- [x] 2.2 成功后留在预测页；列表态不加整卡点击

## 3. 验收

- [x] 3.1 网格：父事件选子后添加；叶子直接添加；开关不误触
- [x] 3.2 喂养页原事件格回归正常
